import React from 'react';
import Title from '../../Title/Title';

const Detail = () => {
	const Content = '';

	return (
		<>
			<div>{Title}</div>
			<div>{Content}</div>
		</>
	);
};

export default Detail;
