import React from 'react';
import Title from '../../Title';

const Detail = () => {
	const content = '';

	return (
		<>
			<div>{Title}</div>
			<div>{content}</div>
		</>
	);
};

export default Detail;
