import React from 'react';
import Title from '../../Title/Title';

const Detail = () => {
	const Content = '';

	return (
		<>
			<Title text={} image={}/>
			<div>{Content}</div>
		</>
	);
};

export default Detail;
